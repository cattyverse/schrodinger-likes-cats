# 概要

## 後で

公開して検索に出したい段階で。Jekyll の `jekyll-sitemap` は使わない。

- サイトマップ: `@astrojs/sitemap`。`site` と `base` は既にある。`astro build` で `sitemap-index.xml` が出る
- Google Search Console: URL プレフィックス `https://cattyverse.github.io/schrodinger-likes-cats/` を登録し、所有権を確認する（HTML ファイル）
- サイトマップ URL を Search Console に 1 回送る: `https://cattyverse.github.io/schrodinger-likes-cats/sitemap-index.xml`

デプロイのたびに Google へ ping はしない（廃止済み）。Search Console API もこの規模では不要。
